from setuptools import setup

setup(
    name='eventedpy',
    version='0.1.7',
    description='Simple, tight event loop',
    author='Jack Stephenson <jackatbancast>',
    author_email='jack@bancast.net',
    url='https://github.com/jackatbancast/eventedpy.git',
    packages=['eventedpy'],
)
